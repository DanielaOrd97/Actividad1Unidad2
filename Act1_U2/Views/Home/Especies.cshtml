@model Act1_U2.Models.ViewModel.EspeciesViewModel;
@{
    Layout = "MainLayout";
}

<main>

  @*  @foreach (var i in Model)
    {
        <h1>
            <img src="clases/@(i.IdClase).png" /> @i.NombreClase
        </h1>

        <div class="listaEspecies2">
            <a href="especie.html">

                <img src="especies/1.jpg" title="@i.NombreEspecie" />



            </a>
        </div>
    }*@

    <h1>
        <img src="clases/@(Model.IdClase).png" /> @Model.NombreClase
    </h1>

    <div class="listaEspecies2">
        <a href="especie.html">

            @foreach (var item in Model.Especies)
            {
                <img src="especies/1.jpg" title="@item.Nombre" />
            }
        </a>
    </div>

    <script>

        var c = document.querySelector("main h1 img").cloneNode();
        document.querySelector("main h1").appendChild(c);
    </script>

</main>

